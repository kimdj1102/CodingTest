import Foundation

func solution(_ sides:[Int]) -> Int {
    let sidesSorted = sides.sorted()
    let a = sidesSorted[0]
    let b = sidesSorted[1]
    var n = 0
    
    b - a < n && n <= b 
    
    
    return 0
}
